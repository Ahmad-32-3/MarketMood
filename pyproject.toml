[project]
name = "regimes"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["numpy", "hmmlearn"]

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["src"]
